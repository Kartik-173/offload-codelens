const {
  NODE_ENV,
  ENVIRONMENT,
  PORT,
  LOG_LEVEL,
  OTEL_EXPORTER_OTLP_ENDPOINT,
  LOKI_URL,
  GITHUB_OAUTH_NAME,
  BITBUCKET_OAUTH_NAME,
  SLACK_OAUTH_NAME,
  VAULT_ADDR,
  VAULT_TOKEN,
  SONAR_HOST_URL,
  SONAR_TOKEN,
  S3_BUCKET_NAME,
  ACCESSKEYID,
  SECRETACCESSKEY,
  AWS_REGION,
  USER_POOL_ID,
  COGNITO_CLIENT_ID,
  COGNITO_CLIENT_SECRET,
  COGNITO_REDIRECT_URI,
  COGNITO_REDIRECT_URI_DEV,
  COGNITO_LOGIN_PAGE_URL,
  CODELENS_URL,
  N8N_ADDR,
  PROWLER_BASE_URL,
  OPEN_SEARCH_URL,
  OPEN_SEARCH_USERNAME,
  OPEN_SEARCH_PASSWORD,
  SERVICE_NAME,
  VEGETA_API_URL,
  // Cassandra Configuration
  CASSANDRA_CONTACT_POINTS,
  CASSANDRA_PORT,
  CASSANDRA_DATACENTER,
  CASSANDRA_USERNAME,
  CASSANDRA_PASSWORD,
  CASSANDRA_KEYSPACE
} = process.env;

const IS_DEV = NODE_ENV === 'development';
const IS_PROD = NODE_ENV === 'production';

const config = {
  NODE_ENV,
  ENVIRONMENT,
  PORT,
  LOG_LEVEL,
  OTEL_EXPORTER_OTLP_ENDPOINT,
  LOKI_URL,
  GITHUB_OAUTH_NAME,
  BITBUCKET_OAUTH_NAME,
  SLACK_OAUTH_NAME,
  VAULT_ADDR,
  VAULT_TOKEN,
  SONAR_HOST_URL,
  SONAR_TOKEN,
  S3_BUCKET_NAME,
  ACCESSKEYID,
  SECRETACCESSKEY,
  AWS_REGION,
  USER_POOL_ID,
  COGNITO_CLIENT_ID,
  COGNITO_CLIENT_SECRET,
  COGNITO_REDIRECT_URI: IS_DEV ? COGNITO_REDIRECT_URI_DEV : COGNITO_REDIRECT_URI,
  COGNITO_LOGIN_PAGE_URL,
  CODELENS_URL,
  N8N_ADDR,
  PROWLER_BASE_URL,
  OPEN_SEARCH_URL,
  OPEN_SEARCH_USERNAME,
  OPEN_SEARCH_PASSWORD,
  SERVICE_NAME,
  VEGETA_API_URL,
  // Environment helpers
  IS_DEV,
  IS_PROD,
  // Cassandra Configuration
  cassandra: {
    contactPoints: CASSANDRA_CONTACT_POINTS,
    port: CASSANDRA_PORT,
    datacenter: CASSANDRA_DATACENTER,
    username: CASSANDRA_USERNAME,
    password: CASSANDRA_PASSWORD,
    keyspace: CASSANDRA_KEYSPACE
  }
};

module.exports = config;
